import React from 'react';

function Header() {
    return (
        <header style={headerStyle}>

        </header>
    )
}

const headerStyle = {
    background: '#000',
    color: '#000',
    textAlign: 'center',
    padding: '20px',
    marginBottom: '10px',
}

export default Header;
